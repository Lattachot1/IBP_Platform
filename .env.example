# Copy to .env and change the password before `docker compose up`.
# docker-compose.yml falls back to the values below when .env is absent.
MSSQL_SA_PASSWORD=YourStrong@Password!
DATABASE_URL=sqlserver://sa:YourStrong@Password!@mssql:1433?database=IBP_DB
